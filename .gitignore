# Local database restore points (not for the repo — use database/amari_db.sql instead)
backups/

# OS / editor clutter
Thumbs.db
.DS_Store
.vscode/

# Real credentials — never commit (use secrets.example.php as the template)
includes/secrets.php
